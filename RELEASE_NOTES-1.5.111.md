# Enigma 2.0 – 1.5.111

X.com home timeline stays smoother while scrolling. Profile pages are unchanged in scope (still heavier).

## English (AMO)

- **Fix:** The home feed no longer restyles every new tweet card. Only posts that look like cipher are processed, and scans stay inside the timeline (not the composer).
- **Fix:** Selection reporting from the lightweight bridge no longer runs on X/YouTube (that IPC on every click slowed `/home` after 1.5.110).
- Same AES-256-GCM / PBKDF2. No data collection. No new permissions.

## Deutsch (AMO)

- **Fix:** Die Home-Timeline restyled nicht mehr jede neue Tweet-Karte. Nur Beiträge, die nach Cipher aussehen, werden verarbeitet; Scans bleiben in der Timeline (nicht im Composer).
- **Fix:** Die Bridge sendet auf X/YouTube keine Selection-Messages mehr (das IPC bei jedem Klick hat `/home` nach 1.5.110 gebremst).
- Gleiche AES-256-GCM / PBKDF2. Keine Datenerfassung. Keine neuen Berechtigungen.

## Short paste (AMO version notes, EN)

X.com home timeline is smoother while scrolling. Same AES-256-GCM, no data collection.

## Kurz (AMO Versionshinweise, DE)

Die X-Hauptseite bleibt beim Scrollen flüssiger. Gleiche AES-256-GCM, keine Datenerfassung.
